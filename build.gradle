
plugins {
    id 'java'
    id "info.solidsoft.pitest" version "1.3.0"
    id "org.sonarqube" version "2.6.2"
    id "com.alexleventer.slack" version "1.0.3"
    id "jacoco"
}



group 'matrix'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}



jacocoTestCoverageVerification {
    violationRules {
        rule {
            limit {
                minimum = 0.8
            }
        }
    }
}

task generateMatrixAPI (type:Copy){
    from('build/reports/tests'){
        into 'TestReports'
    }
    from('build/reports/jacoco'){
        into 'TestQualityReports'
    }
    from('build/reports/pitest'){

        into 'TestQualityReports'
    }
    from('build/docs/javadoc'){

        into 'Documentation'
    }
    from('build/libs'){
        into 'API'
    }
    into 'C:/matMatrixRelease/Matrix_v_${version}'
}

task deployJar{
    repositories {
        maven {
            credentials {
                username "myMavenRepo"
                password "joker.15W"
            }
            url "https://mymavenrepo.com/repo/Fy2IY6gZtwWpwmY8SOz5/"
        }
    }
}



slack {
    webhookUrl "https://hooks.slack.com/services/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"
    username "Gradle"
    shouldMonitor "deployJar"
}
test.finalizedBy("pitest","jacocoTestCoverageVerification","jacocoTestReport")
project.tasks["deployJar"].dependsOn("generateMatrixAPI")
project.tasks["generateMatrixAPI"].dependsOn("build")